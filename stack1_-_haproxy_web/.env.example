# =============================================================================
# Stack1 - HAProxy + Web
# =============================================================================

# Git checkout root and persistent runtime root are intentionally separate.
STACKS_ROOT=/opt/docker/stacks
BASE_PATH=/opt/docker/runtime

# Puertos publicos de HAProxy.
HAPROXY_HTTP_PORT=80
HAPROXY_HTTPS_PORT=443

# Dominio raiz.
ROOT_HOSTNAME=casa.lan

# Backends que se parametrizan desde Docker Compose.
WEB_TARGET=web:80
SEARCH_HOSTNAME=buscar
SEARCH_TARGET=searxng:8080
CHAT_HOSTNAME=chat
CHAT_TARGET=open-webui:8080
GIT_HOSTNAME=git
GIT_TARGET=gitea:3000

# Additional HAProxy backends
GWIA_HOSTNAME=gwia
GWIA_TARGET=litellm:4000
HOMELAB_HOSTNAME=homelab
HOMELAB_TARGET=dockhand:3000
AGENTIA_HOSTNAME=agentia
AGENTIA_TARGET=hermes:9119
XYOPS_HOSTNAME=xyops
XYOPS_TARGET=xyops:5522
